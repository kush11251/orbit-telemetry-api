# Orbit Telemetry API
## Introduction
This project provides a high-performance telemetry aggregation engine built using Node.js and Express.js.
## Installation
1. Clone the repository
2. Run `npm install` to install dependencies
## Running the Application
1. Use the command `npm start` to start the server
2. The server will be listening on port 3000